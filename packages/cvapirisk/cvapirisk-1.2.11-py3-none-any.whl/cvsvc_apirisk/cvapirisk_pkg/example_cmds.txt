# Please refer to the documentation for more details

# Use a zip of specs
$ cvapirisk eval_risk -z orangebank.zip -i cv_rules.json -o /tmp/cvreport.json

# Use a single spec
$ cvapirisk eval_risk -s orangebank_user.json -i cv_rules.json -o /tmp/cvreport.json

# Use custom rules as well
$ cvapirisk eval_risk -s orangebank_user.json -o /tmp/cvreport.json -i cv_rules.json -r custom_rules.json
# With CV rules disabled
$ cvapirisk eval_risk -s orangebank_user.json -o /tmp/cvreport.json -r custom_rules.json

# CICD capability
$ cvapirisk eval_risk -s orangebank_user.json -o /tmp/cvreport.json -r custom_rules.json -c cicd.cfg

# Show trending risk
$ cvapirisk trend_risk orangebank_user_orig.json orangebank_user.json -i cv_rules.json -o /tmp/cvreport.json
