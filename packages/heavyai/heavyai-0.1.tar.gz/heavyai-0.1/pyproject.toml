[tool.poetry]
name = "heavyai"
version = "0.1"
description = "heavyai"
authors = ["Heavy.AI"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.7"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
