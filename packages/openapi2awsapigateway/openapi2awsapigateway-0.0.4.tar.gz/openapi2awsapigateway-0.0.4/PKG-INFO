Metadata-Version: 2.1
Name: openapi2awsapigateway
Version: 0.0.4
Summary: Adapt OpenAPI and Swagger files to include AWS API Gateway Integrations
Home-page: https://github.com/athomegroup/openapi2awsapigateway
Author: Joao Lopes
Author-email: joao.lopes@athome.eu
License: unlicensed
Platform: UNKNOWN
Description-Content-Type: text/markdown
License-File: LICENSE.md

# Adapt OpenAPI and Swagger files to include AWS API Gateway Integrations

## Installation

```
$ pip install -r requirements.txt

$ python setup.py install
```

## Development

This project includes a number of helpers in the `Makefile` to streamline common development tasks.

### Environment Setup

The following demonstrates setting up and working with a development environment:

```
### create a virtualenv for development

$ make virtualenv

$ source env/bin/activate


### run openapi2awsapigateway cli application

$ openapi2awsapigateway --help


### run pytest / coverage

$ make test
```


### Releasing to PyPi

Before releasing to PyPi, you must configure your login credentials:

**~/.pypirc**:

```
[pypi]
username = YOUR_USERNAME
password = YOUR_PASSWORD
```

Then use the included helper function via the `Makefile`:

```
$ make dist

$ make dist-upload
```

## Deployments

### Docker

Included is a basic `Dockerfile` for building and distributing `OpenAPI to AWS API Gateway`,
and can be built with the included `make` helper:

```
$ make docker

$ docker run -it openapi2awsapigateway --help
```


