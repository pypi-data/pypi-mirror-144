[tool.poetry]
name = "toolstack-core"
version = "0.1.1"
description = ""
authors = ["Pascal Prins <pascal.prins@toolstack.ninja>"]
packages = [{include = "toolstack"}]

[tool.poetry.dependencies]
python = "^3.9"
click = "^8.0.4"
PyYAML = "^5.4.1"
beautifulsoup4 = "^4.10.0"
requests = "^2.27.1"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
