
import setuptools

setuptools.setup(
    name="smart-xontrib-readable-traceback",
    version="1.0",
    packages=setuptools.find_packages(),
    install_requires=['colorama>=0.3.7', 'backtrace'],
)
